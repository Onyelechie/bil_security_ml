name: CI

on:
  push:
    branches: [ main, Server ]
  pull_request:
    branches: [ main, Server ]

jobs:
  runtime-smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install lightweight runtime dependencies
        run: |
          python -m pip install --upgrade pip
          # Use the lightweight dev requirements for smoke checks to avoid heavy ML/CV packages
          pip install -r requirements-dev.txt

      - name: Smoke import (runtime)
        env:
          PYTHONPATH: ${{ github.workspace }}/src
          DATABASE_URL: sqlite:///./test.db
        run: |
          python -c "import server.main; print('✓ server.main import OK')"
          python -c "import edge_agent.main; print('✓ edge_agent.main import OK')"

  tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install dev dependencies (lightweight)
        run: |
          python -m pip install --upgrade pip
          # Use the lightweight dev requirements to avoid large ML/CV packages in CI unit tests
          pip install -r requirements-dev.txt

      - name: Run DB migrations
        env:
          DATABASE_URL: sqlite:///./test.db
          PYTHONPATH: ${{ github.workspace }}/src
        run: |
          # Run Alembic migrations so tests run against the latest schema
          alembic upgrade head

      - name: Run tests (with coverage)
        env:
          DATABASE_URL: sqlite:///./test.db
          PYTHONPATH: ${{ github.workspace }}/src
        run: |
          python -m pytest tests/ -v --tb=short --strict-markers --cov=src --cov-report=term-missing

