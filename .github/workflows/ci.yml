name: CI

on:
  push:
    branches: [ main, Server ]
  pull_request:
    branches: [ main, Server ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Run tests
      run: |
        python -m pytest tests/ -v --tb=short --strict-markers
      env:
        DATABASE_URL: sqlite:///./test.db

    - name: Check imports
      run: |
        python -c "import src.server.main; print('✓ Server imports successfully')"
        python -c "import src.server.db; print('✓ Database imports successfully')"
        python -c "import src.server.schemas; print('✓ Schemas import successfully')"

    - name: Check code formatting (optional)
      run: |
        # Uncomment when black and isort are added to requirements
        # black --check src/
        # isort --check-only src/
      continue-on-error: true