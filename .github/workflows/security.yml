name: Security Scan

on:
  pull_request:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  dependency-audit:
    name: Dependency Vulnerability Scan (pip-audit)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install deps (runtime + dev tools)
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt -r requirements-dev.txt

      - name: Run pip-audit
        run: |
          # Temporary ignore: CVE-2026-4539 affects Pygments 2.19.2; no fixed
          # release available at the time of writing. Remove once a patched
          # version is published and upgraded.
          pip-audit -r requirements.txt --ignore-vuln CVE-2026-4539
          pip-audit -r requirements-dev.txt --ignore-vuln CVE-2026-4539

  bandit-sast:
    name: Static Code Scan (bandit)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install bandit
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-dev.txt

      - name: Run bandit on src/
        run: |
          bandit -r src -q
