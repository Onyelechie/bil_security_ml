[pytest]
testpaths = tests
pythonpath = . src
addopts = -v --tb=short --strict-markers --cov=src --cov-report=term-missing
markers =
    integration: tests that require real model weights and perform inference
filterwarnings =
	ignore:Failed to initialize NumPy:UserWarning
