# Lightweight development requirements (omit heavy ML packages)

# --- Core / Web API ---
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
pydantic-settings>=2.2.1
sqlalchemy>=2.0.25
alembic>=1.13.1
python-multipart>=0.0.7
httpx>=0.27.0

# --- Data & Utilities ---
numpy>=1.26.0
pandas>=2.0.0
psutil>=5.9.0
PyYAML>=6.0
requests>=2.28.0

# --- Development & Type Stubs ---
mypy>=1.9.0
types-requests>=2.32.0

# --- Testing ---
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.0.0

# Note: For full ML / CV features (torch, torchvision, ultralytics, opencv, etc.)
# install the main `requirements.txt` on a machine with appropriate wheels or use conda.
